package com.tpi.gateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuración adicional de Swagger UI.
 * La configuración principal está en application.yml
 */
@Configuration
public class SwaggerConfig {
    // Configuración movida a application.yml para evitar dependencias circulares
}
